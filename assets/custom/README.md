# Custom Game Visuals

Put your own `.gif`, `.jpg`, `.jpeg`, `.png`, `.webp`, or `.avif` files in this folder.

Then open `visuals.js` in the main game folder and replace a scene path:

```js
alley: "assets/custom/my-alley.gif"
```

Keep the scene ID before the colon unchanged.
